# Ensure That SSH Access Is Restricted From the Internet

| Provider | Category |
| -------- | -------- |
| Google Cloud Platform | Network security |

## Description

This control checks that no enabled, ingress `google_compute_firewall` rule allows TCP (or "all protocols") traffic on port 22 from the unrestricted source range `0.0.0.0/0`. A rule is only flagged when it is not `disabled`, its direction resolves to `INGRESS` (the provider default when `direction` is omitted), `0.0.0.0/0` is among its `source_ranges`, and its `allow` blocks permit port 22 either explicitly, via an empty (all-ports) `ports` list, or via a port range that includes 22 — for a protocol of `tcp` or `all`.

SSH is one of the most commonly targeted services by internet-wide port scanners and automated brute-force/credential-stuffing bots. Allowing SSH from any source address means every internet-connected attacker can attempt to authenticate against the instance, relying solely on credential strength (or key-based auth) as the only barrier. Restricting SSH to specific, trusted CIDR ranges (such as a corporate VPN or bastion host) removes this exposure entirely for anyone outside those ranges.

This rule is covered by the [3-6-restrict-ssh](https://github.com/hashicorp/prewritten-terraform-policy-library/blob/main/policies/gcp/network/3-6-restrict-ssh.policy.hcl) policy.

## Policy Results

```bash
trace:
   # 3-6-restrict-ssh.policytest.hcl... 
   running
   # resource.google_compute_firewall.pass_trusted_source_tcp_22... 
   running
   # resource.google_compute_firewall.pass_trusted_source_tcp_22... 
   pass
   # resource.google_compute_firewall.fail_unrestricted_tcp_22... 
   running
   # resource.google_compute_firewall.fail_unrestricted_tcp_22... 
   pass
   # resource.google_compute_firewall.fail_unrestricted_all_ports_omitted... 
   running
   # resource.google_compute_firewall.fail_unrestricted_all_ports_omitted... 
   pass
   # resource.google_compute_firewall.fail_unrestricted_range_contains_22... 
   running
   # resource.google_compute_firewall.fail_unrestricted_range_contains_22... 
   pass
   # resource.google_compute_firewall.pass_disabled_rule... 
   running
   # resource.google_compute_firewall.pass_disabled_rule... 
   pass
   # resource.google_compute_firewall.pass_egress_rule... 
   running
   # resource.google_compute_firewall.pass_egress_rule... 
   pass
   # resource.google_compute_firewall.pass_range_excludes_22... 
   running
   # resource.google_compute_firewall.pass_range_excludes_22... 
   pass
   # resource.google_compute_firewall.pass_udp_22... 
   running
   # resource.google_compute_firewall.pass_udp_22... 
   pass
   # resource.google_compute_firewall.fail_omitted_optional_defaults... 
   running
   # resource.google_compute_firewall.fail_omitted_optional_defaults... 
   pass
   # resource.google_compute_firewall.pass_null_source_ranges... 
   running
   # resource.google_compute_firewall.pass_null_source_ranges... 
   pass
   # resource.google_compute_firewall.pass_null_allow... 
   running
   # resource.google_compute_firewall.pass_null_allow... 
   pass
   # 3-6-restrict-ssh.policytest.hcl... 
   pass
```

---
